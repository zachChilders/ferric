// Complex test with variables, functions, and control flow
fn max(a: Int, b: Int) -> Int {
    if a > b {
        a
    } else {
        b
    }
}

let x = 10
let y = 20
println(int_to_str(max(x, y)))
